# Setup fzf
# ---------
if [[ ! "$PATH" == */home/jazzar/.fzf/bin* ]]; then
  PATH="${PATH:+${PATH}:}/home/jazzar/.fzf/bin"
fi

# Auto-completion
# ---------------
# source "/home/jazzar/.fzf/shell/completion.zsh"

# Key bindings
# ------------
# source "/home/jazzar/.fzf/shell/key-bindings.zsh"
